// Movido pra `com.billfolder.android.ui.components.BillFolderTotalCard` —
// o componente virou genuinamente reutilizável (recebe `label` como
// parâmetro) e mora em ui/components/ junto com os outros do design system.
//
// TODO: deletar este arquivo manualmente (não consigo deletar via tooling
// do shell sandbox). No Android Studio: clica direito no arquivo → Delete.
package com.billfolder.android.ui.screens.dailyexpenses.components
