package com.billfolder.android.ui.theme

import androidx.compose.ui.graphics.Color

// ============================================================================
// Brand
// ============================================================================
// Verde principal — usado em CTAs, hero numbers, ícones de inflow.
val BillGreen = Color(0xFF86BC65)
val BillGreenDim = Color(0xFF6FA050)

// Vermelho contábil pra outflows / estouros de orçamento.
val BillRed = Color(0xFFE65A5A)

// Amarelo pra estados intermediários (overdue/pendente).
val BillAmber = Color(0xFFE0B53A)

// ============================================================================
// Surfaces (dark theme único)
// ============================================================================
// Background quase-preto com leve viés esverdeado pra "casar" com o BillGreen.
val SurfaceBlack    = Color(0xFF0A0D0A)
val SurfaceElevated = Color(0xFF14181A)
val SurfaceCard     = Color(0xFF1A1F1D)
val SurfaceHigh     = Color(0xFF24292B)

// ============================================================================
// On-* / texto
// ============================================================================
val TextPrimary   = Color(0xFFEDEFEA)
val TextSecondary = Color(0xFF9BA39B)
val TextDisabled  = Color(0xFF5A615C)

val OutlineSubtle = Color(0xFF2A2F2C)
